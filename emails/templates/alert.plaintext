The following item(s) are below their par Level
{% for item in items %}
 {{item.name}} is now at {{item.level}} {{item.unit|default_if_none:"units"}}. The par level is set at {{item.par}} {{item.unit|default_if_none:"units"}}.
{% endfor %}
